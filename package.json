{
  "name": "create-hteacher",
  "version": "1.0.12",
  "description": "design for hteacher.net",
  "type": "module",
  "bin": "dist.cjs",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --outfile=dist.cjs --platform=node",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cli",
    "vue",
    "react"
  ],
  "author": "LiWenShuo",
  "license": "ISC",
  "devDependencies": {
    "cross-spawn": "^7.0.3",
    "ejs": "^3.1.9",
    "gradient-string": "^2.0.2",
    "prompts": "^2.4.2"
  },
  "dependencies": {
    "esbuild": "^0.20.1"
  }
}
